Predict a horizontal CSS flex row 1000 units wide, with min-width zero and no wrapping. Input includes item properties and sums; weightedShrink means basis times shrink. Output only JSON with integer offset, extra, and widths array. Offset is the first left position. Extra is total additional spacing between items beyond the input gaps, zero for one item. Widths are nonnegative; preserve overflow and negative offset.
